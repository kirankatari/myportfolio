# Copy to .env on the VPS (in this folder). Never commit .env.
# Get a key from https://platform.openai.com/api-keys

OPENAI_API_KEY=sk-proj-replace-with-your-key

# Optional — only if you changed server.js defaults
# PORT=3000
# HOST=127.0.0.1

{
  "name": "portfolio-chat-api",
  "version": "1.0.0",
  "private": true,
  "description": "Server-side /api/chat for portfolio (OpenAI key never sent to browser)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.21.0"
  }
}
